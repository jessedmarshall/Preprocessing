function [setDiffIdx] = getIdxMatrixDiff(setAMatrix,setBMatrix,varargin)
	% NOT FINISHED: obtains which indicies in one matrix match those in another
	% biafra ahanonu
	% started: 2013.11.22
	% inputs
		%
	% outputs
		%
	% changelog
		%
	% TODO
		%

	%========================
	options.exampleOption = 'doSomething';
	% get options
	options = getOptions(options,varargin);
	% display(options)
	% unpack options into current workspace
	% fn=fieldnames(options);
	% for i=1:length(fn)
	% 	eval([fn{i} '=options.' fn{i} ';']);
	% end
	%========================

